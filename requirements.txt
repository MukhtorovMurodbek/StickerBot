python-telegram-bot[job-queue]>=21.3
Pillow>=10.0
python-dotenv>=1.0

# psycopg's connection pool. Every query in db.py and family_link.py goes
# through it -- see the comment block at the top of db.py for why a bot that
# opened a fresh connection per query is the single most expensive thing this
# family did to its database.
psycopg[binary,pool]>=3.2
